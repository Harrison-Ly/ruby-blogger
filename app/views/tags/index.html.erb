<h1>Articles by Tag</h1>

<ul id="tag">
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
      <%= link_to "[Delete]", tag_path(tag), method: "delete" %>
    </li>
  <% end %>
</ul>